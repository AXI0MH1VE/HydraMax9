# ==============================================================================
# HydraMax9 - Deterministic AI Substrate Configuration
# ==============================================================================
# This file contains environment variables for configuring HydraMax9's
# operational behavior, security posture, and compliance requirements.
#
# SECURITY WARNING: Never commit actual API keys or secrets to version control.
# Copy this file to .env and configure with your actual credentials.
# ==============================================================================

# ------------------------------------------------------------------------------
# AI Model Configuration
# ------------------------------------------------------------------------------
# Gemini AI API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
API_KEY=your_gemini_api_key_here

# Backend API endpoint for orchestration layer
# Example: https://api.yourdomain.com or http://localhost:3000
BACKEND_BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Security & Risk Management
# ------------------------------------------------------------------------------
# Enable/disable real-time security entropy module
# Recommended: true for production, staging, and regulated environments
# Regulatory Alignment: EU AI Act (Art. 9 - Risk Management), NIST AI RMF
SECURITY_MODULE_ENABLED=true

# Entropy threshold for anomaly detection (0.0 - 1.0)
# Higher values = more sensitive to prompt injection, data leakage, adversarial inputs
# Recommended: 0.85 for high-security, 0.70 for balanced, 0.55 for permissive
# Regulatory Alignment: Financial Services - Model Risk Management (SR 11-7)
ENTROPY_THRESHOLD=0.85

# Anomaly detection enforcement mode
# Options: strict | lenient | monitor-only
# - strict: Block suspicious requests immediately
# - lenient: Flag but allow with logging
# - monitor-only: Log without intervention (compliance testing)
# Regulatory Alignment: HIPAA Security Rule (§164.308 - Access Controls)
ANOMALY_DETECTION_MODE=strict

# Maximum allowed information rate (bits/token) before circuit-breaking
# Prevents data exfiltration through encoding attacks
# Recommended: 1.5 for strict, 2.5 for balanced
INFORMATION_RATE_LIMIT=1.5

# ------------------------------------------------------------------------------
# Audit & Compliance
# ------------------------------------------------------------------------------
# Audit log retention period (days)
# Regulatory requirements:
# - SEC 17a-4: 7 years (2555 days) for financial records
# - GDPR: Varies by purpose (typically 6-24 months)
# - HIPAA: 6 years (2190 days) minimum
# - SOX: 7 years (2555 days) for public companies
# Recommended: 2555 days for financial/healthcare, 730 for general enterprise
AUDIT_LOG_RETENTION_DAYS=2555

# Enable comprehensive audit trail for all AI interactions
# Captures: prompts, responses, model selection, latency, entropy scores
# Regulatory Alignment: EU AI Act (Art. 12 - Record-Keeping), SOC 2 Type II
AUDIT_TRAIL_ENABLED=true

# Audit log storage location
# Options: local | s3 | azure-blob | gcs
AUDIT_STORAGE_BACKEND=local

# Local audit log directory (if AUDIT_STORAGE_BACKEND=local)
AUDIT_LOG_PATH=/var/log/hydramax9/audit

# ------------------------------------------------------------------------------
# Operational SLA & Rate Limiting
# ------------------------------------------------------------------------------
# Maximum API requests per minute per user/session
# Prevents abuse and ensures fair resource allocation
# Recommended: 100 for internal users, 20 for external APIs
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# Request timeout (milliseconds)
# Balance between user experience and resource protection
REQUEST_TIMEOUT_MS=30000

# Maximum concurrent sessions per deployment
# Controls resource utilization and cost
MAX_CONCURRENT_SESSIONS=50

# ------------------------------------------------------------------------------
# Transport Security (TLS/mTLS)
# ------------------------------------------------------------------------------
# Path to TLS certificate for HTTPS
# Required for production deployments handling regulated data
# Regulatory Alignment: PCI DSS 4.0 (Req. 4 - Encrypt Data in Transit)
TLS_CERTIFICATE_PATH=/etc/ssl/certs/hydramax9.crt

# Path to TLS private key
TLS_PRIVATE_KEY_PATH=/etc/ssl/private/hydramax9.key

# Enable mutual TLS (mTLS) for client authentication
# Recommended for zero-trust architectures and high-security environments
MTLS_ENABLED=false

# Path to client certificate authority for mTLS
MTLS_CA_PATH=/etc/ssl/ca/client-ca.crt

# ------------------------------------------------------------------------------
# Data Residency & Sovereignty
# ------------------------------------------------------------------------------
# Geographic region for data processing
# Options: us-east | us-west | eu-central | eu-west | asia-pacific | air-gapped
# Regulatory Alignment: GDPR (Art. 44-50 - Data Transfers), China PIPL
DATA_RESIDENCY_REGION=us-east

# Enable air-gapped mode (no external API calls)
# Requires local model deployment via Ollama, LocalAI, or vLLM
AIR_GAPPED_MODE=false

# Local model inference endpoint (if AIR_GAPPED_MODE=true)
LOCAL_MODEL_ENDPOINT=http://localhost:11434

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Node environment
# Options: development | staging | production
NODE_ENV=development

# Development server port
PORT=5173

# Application version (auto-populated by CI/CD)
VITE_APP_VERSION=1.0.0

# ------------------------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------------------------
# Enable neural graph visualization
FEATURE_NEURAL_GRAPH=true

# Enable performance monitoring dashboard
FEATURE_PERFORMANCE_MONITOR=true

# Enable bootstrap sequence on startup
FEATURE_BOOTSTRAP_SEQUENCE=true

# Enable experimental multi-model orchestration (Phase 1 Roadmap)
FEATURE_MULTI_MODEL_ORCHESTRATION=false

# ------------------------------------------------------------------------------
# Telemetry & Observability
# ------------------------------------------------------------------------------
# Enable telemetry export to monitoring systems
# Options: prometheus | datadog | newrelic | disabled
TELEMETRY_BACKEND=prometheus

# Telemetry export endpoint
TELEMETRY_ENDPOINT=http://localhost:9090

# Enable detailed performance tracing
PERFORMANCE_TRACING_ENABLED=true

# Sample rate for distributed tracing (0.0 - 1.0)
TRACE_SAMPLE_RATE=0.1

# ------------------------------------------------------------------------------
# End of Configuration
# ------------------------------------------------------------------------------
